# Screenshots (evidence)

Add PNG screenshots here to prove the interfaces run. Suggested:

- `dashboard-green.png`      — the Mirth Dashboard showing the channel started, Received > 0
- `server-log-hl7.png`       — the Server Log line "Result for Umar, Aisha: GLU = 95"
- `fhir-observation.png`     — the generated FHIR Observation JSON
- `fhir-201-created.png`     — the HTTP 201 Created response from the FHIR server

Then reference the best one near the top of the main README so it shows on the repo home page.
